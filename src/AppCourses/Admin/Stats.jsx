import React from "react";
import { LineChart } from "@mui/x-charts/LineChart";

const Stat = ({ totalLikes = 0, totalViews = 0 }) => {

    return (
        <div >
            Stats
        </div>
    );
};

export default Stat;
